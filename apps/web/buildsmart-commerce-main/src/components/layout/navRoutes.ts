// Routes whose top section is a full-bleed dark hero. The navbar overlays the
// hero there (transparent + scrim); everywhere else it is solid from the top,
// and the layout adds top padding so content clears the fixed bar.
export const darkHeroRoutes = ["/", "/services/architectural", "/services/interior"];
